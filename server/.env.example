# VendingPreneurs Server Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED - Server will not start without these in production
# =============================================================================

# Neon Database Connection String
# Get this from your Neon dashboard: https://console.neon.tech
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# JWT Secret for authentication tokens
# Generate a strong random string (32+ characters)
# Example: openssl rand -base64 32
JWT_SECRET=your-super-secret-key-change-this

# =============================================================================
# OPTIONAL - Defaults provided
# =============================================================================

# Server port (default: 3001)
PORT=3001

# Environment (development/production)
NODE_ENV=development

# CORS origins (comma-separated, default: localhost)
# Add your Electron app origin and admin dashboard URL
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,file://

# =============================================================================
# SEED DATA - Used when running npm run db:seed
# =============================================================================

# Default admin account (created during seeding)
ADMIN_EMAIL=admin@vendingpreneurs.com
ADMIN_PASSWORD=changeme123

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
#
# For Vercel deployment:
# 1. Add DATABASE_URL from Neon
# 2. Add JWT_SECRET (generate new one for production)
# 3. Add CORS_ORIGINS with your deployed URLs
#
# For Railway/Render/Fly.io:
# Same environment variables, configured via dashboard
